/** @type {import('next').NextConfig} */
const nextConfig = {
    // images: {
    //     remotePatterns: [
    //         {
    //             protocol: 'http',
    //             hostname: '*', // Replace with your actual hostname
    //         },
    //     ],
    // },
};

export default nextConfig;
